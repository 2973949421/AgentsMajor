{
  "name": "@agent-major/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/clean-next.mjs && next build",
    "dev": "next dev",
    "pretypecheck": "pnpm --filter @agent-major/shared build && pnpm --filter @agent-major/db build && pnpm --filter @agent-major/llm build && pnpm --filter @agent-major/core build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm --filter @agent-major/shared build && pnpm --filter @agent-major/db build && pnpm --filter @agent-major/llm build && pnpm --filter @agent-major/core build",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@agent-major/core": "workspace:*",
    "@agent-major/db": "workspace:*",
    "@agent-major/materials": "workspace:*",
    "next": "^15.3.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.6",
    "@types/react-dom": "^19.1.5"
  }
}
